# Audio Assets

Place word-level mp3 files here using this structure:

Audio/<reciter-folder>/<word-key-with-colons-replaced-by-underscores>.mp3

Example:
Audio/alafasy/67_al-mulk.mp3

Reciter folders should match `Reciter.folderName` values in `SettingsModels.swift`.
